# Notebooks

`medvlm_training_original.ipynb` is the original exploratory Kaggle notebook used to recover the model code, training logs, qualitative examples, and generated figures.

The notebook is intentionally preserved as historical provenance with outputs stripped for safe public redistribution. The cleaned and reusable implementation lives in `src/medvlm/` and `scripts/`.
